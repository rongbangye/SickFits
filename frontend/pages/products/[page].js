// we want the same thing render out on three didfferent URLs
// This is file based routing at its finest
// we got three different files, but they're all rendering out the same thing

// 2. Allow for dynamic routing
export { default } from './index';
